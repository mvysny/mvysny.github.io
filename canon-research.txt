Canon PIXMA TS5351 / CUPS / TLS - working notes
===============================================

Scratch file for the blog post _posts/2026-08-22-canon-pixma-cups-tls.md.
Not committed. Purpose: stop re-establishing the same context every session.


0. THE TWO MACHINES  (read this first)
--------------------------------------

There are two machines and they are NOT interchangeable.

VM  - this box, where the git repo /home/mavi/work/my/mvysny.github.io lives.
      Has CUPS installed as a snap, but NO printer, NO print queues, and the
      Canon is not reachable on its network.
        * good for: verifying paths, binaries, library linkage, snap layout,
          config-file search order, mount namespaces, log locations.
        * useless for: anything about the printer, TLS handshakes to it,
          whether a queue prints, cups-browsed discovery behaviour.

HOST - the physical machine the printer is actually being set up on. Also runs
       CUPS from the snap. Every experiment involving the printer happens here.
       Claude has no access to it; results arrive via Martin pasting them.

Consequence: when Claude says "verified", it means verified on the VM unless it
explicitly says otherwise. Printer-facing claims can only be verified by Martin
running the command on HOST.


1. PRINTER FACTS  (from the post; don't re-derive)
-------------------------------------------------

model            Canon PIXMA TS5351  (advertises as Canon_TS5300_series)
mDNS hostname    246989000000.local     <- the printer's "Printer Name" field,
                                           OK menu -> System Information
IP               192.168.1.50 (DHCP - do not hardcode in queues)
MAC              74:38:B7:xx:xx:xx
serial           KMHH00159
cert             self-signed, CN=192.168.1.50, issuer CN=CanonIJProduct...,
                 RSA 2048 / SHA-256, valid 2018-01-01 .. 2037-12-31

Root cause: the printer's embedded TLS server sends alert 40 (handshake_failure)
when the ClientHello offers TLS 1.3. Take 1.3 out of the offer and it happily
does TLS 1.2 with ECDHE-SECP256R1 + AES-256-GCM. Secure renegotiation (RFC 5746)
really is missing, but that was a red herring - GnuTLS's default
%PARTIAL_RENEGOTIATION permits the initial handshake and IPP never renegotiates.
OpenSSL objected to the renegotiation thing; CUPS/GnuTLS never did.


2. VERIFIED ON THE VM
---------------------

CUPS is the openprinting snap, not a deb:

    $ snap list cups
    cups  2.4.19-2  1238  latest/stable  openprinting**
    confinement: strict, base: core22

    apt side: only libcups2t64 2.4.16 (for host client programs).
    There is no apt cups-daemon, no /var/log/cups, no /usr/lib/cups/backend/,
    and no bare lpstat / lpadmin / ipptool / cups-config on $PATH.

Snap server root (this is the client.conf answer):

    $ /snap/cups/current/bin/cups-config --serverroot
    /var/snap/cups/common/etc/cups

    $ strings /snap/cups/current/lib/libcups.so.2 | grep -E '/var/snap|client\.conf'
    /var/snap/cups/common/etc/cups
    /var/snap/cups/common/etc/cups/ssl
    %s/client.conf
    %s/.cups/client.conf

    => daemons read /var/snap/cups/common/etc/cups/client.conf
    => /etc/cups/client.conf belongs to the apt libcups2t64 (Gnome print
       dialog etc.) and is never opened by cupsd / cups-browsed / the backend
    => $HOME/.cups/client.conf is still tried FIRST and wins if it opens

The snap ships a client.conf already. On the VM it contains exactly:

    ServerName /run/cups/cups.sock

/snap/cups/current/scripts/run-cups-browsed only greps ServerName out of that
file (to set CUPS_SERVER), so appending unrelated directives is safe. Note the
script's `export CUPS_SERVERROOT=...` line is commented out - the daemons rely
on the compiled-in path shown above.

TLS stack:

    $ readelf -d /snap/cups/current/lib/libcups.so.2 | grep NEEDED
      ... libgnutls.so.30 ...        (no libssl / libcrypto at all)
    $ readelf -d /snap/cups/current/sbin/cups-browsed | grep NEEDED
      libcupsfilters.so.2, libcups.so.2   (nothing else TLS-ish)

    libgnutls resolves out of the core22 base: GnuTLS 3.7.3 there vs 3.8.12 on
    the host - worth remembering when comparing gnutls-cli against CUPS.

    SSLOptions and the MinTLS1.0..MaxTLS1.3 tokens ARE present in the snap's
    libcups, so the client.conf lever exists there.

    CUPS_DEBUG_LOG is absent from the snap's libcups too (built without debug
    printfs), so that diagnostic is unavailable in both layouts.

/etc/gnutls/config DOES reach inside the snap. core22 ships no /etc/gnutls at
all and the snap's fstab bind-mounts only /etc/fonts and /etc/papersize, so this
was worth checking - but reading the file from inside the snap's mount namespace
returns the host's copy verbatim. (Directory listing is AppArmor-denied; reading
the file works.) Verified with:

    echo 'cat /etc/gnutls/config' | snap run --shell cups.lpstat

The VM's /etc/gnutls/config has NO tls1.3 line - only the Ubuntu defaults
(tls1.0, tls1.1, dtls0.9, dtls1.0).

Path translation table (deb -> snap):

    /var/log/cups/error_log            -> /var/snap/cups/current/var/log/error_log
    cups-browsed -> syslog             -> /var/snap/cups/current/var/log/cups-browsed_log
    /etc/cups/{cupsd,cups-files,client,cups-browsed}.conf
                                       -> /var/snap/cups/common/etc/cups/...
    /usr/lib/x86_64-linux-gnu/libcups.so.2
                                       -> /snap/cups/current/lib/libcups.so.2
    /usr/lib/cups/backend/ipp          -> /snap/cups/current/lib/cups/backend/ipp
    systemctl restart cups cups-browsed-> sudo snap restart cups
                                          (snap.cups.cupsd.service,
                                           snap.cups.cups-browsed.service)
    lpstat / lpadmin / ipptool         -> cups.lpstat / cups.lpadmin / cups.ipptool

Proxy mode gotcha: if BOTH a deb CUPS and the snap are installed, the snap's
launcher sees a readable /etc/cups/cupsd.conf and starts in proxy mode - it does
not bind 631, does not run cups-browsed, just mirrors the deb's queues via
cups-proxyd. Check with:

    ls /var/snap/cups/current/var/run/proxy-mode   # exists => debug the DEB CUPS

VM state, for reference: no printers.conf, `cups.lpstat -v` says "No
destinations added", zero Canon hits in cups-browsed_log, no "TLS fatal alert"
anywhere in the snap logs. All the post's printer evidence came from HOST.


3. THE ANSWER - CONFIRMED ON HOST
---------------------------------

*** client.conf IS the fix. Printing works. ***

    file:  /var/snap/cups/common/etc/cups/client.conf
    line:  SSLOptions MinTLS1.2 MaxTLS1.2

Confirmed on HOST by Martin, in this order:
  - cups.ipptool over ipps:// succeeds and retrieves printer attributes
  - printing works
  - ALL /etc/gnutls/config changes reverted first, so the snap client.conf line
    is the only remaining modification to the host OS

So the system-wide GnuTLS override was never necessary. The scoped fix is two
lines in one file inside the snap's own config tree, blast radius = CUPS only.

Mechanism: cupsd exports CUPS_SERVERROOT into every filter and backend it forks,
so the forked ipp backend resolves client.conf to the snap's copy, and
_httpTLSStart() reads cg->ssl_options after populating defaults from that file.
The backend honoring it is what makes printing (not just ipptool) work.

Why the original debugging missed it: the SSLOptions line was written to
/etc/cups/client.conf, which under a snapped CUPS no CUPS process ever opens.
Right lever, wrong file.

DURABILITY - verified by reading /snap/cups/current/scripts/run-cupsd:245-250.
Every cupsd start rewrites client.conf, but it PRESERVES foreign lines:

    touch client.conf
    ( cat client.conf | grep -v ServerName > client.conf.new || true ) && \
        echo ServerName $DOMAINSOCKET > client.conf && \
        cat client.conf.new >> client.conf && \
        rm -f client.conf.new

i.e. it strips ServerName, rewrites it as line 1, then appends everything else
back. The SSLOptions line survives, only reordered to sit after ServerName. And
it lives in $SNAP_COMMON, which is NOT versioned - so it also survives snap
refreshes to a new revision. Nothing to re-apply.

*** REGRESSION AFTER REBOOT - section 3 above is NOT settled. ***

After a reboot on HOST: cups.ipptool over ipps:// still WORKS, but printing does
NOT. So the durability reasoning above holds for the client path and says nothing
about the print path. The pre-reboot "printing works" and the post-reboot
"printing doesn't" may not even be about the same mechanism - see section 3b.


3b. THE REGRESSION - WHAT SPLIT ipptool FROM PRINTING
-----------------------------------------------------

Established: cups.ipptool honors /var/snap/cups/common/etc/cups/client.conf and
reaches the printer over ipps://. That is a libcups CLIENT running as Martin.
Printing involves two other processes - cups-browsed (long-running daemon) and
the ipp/ipps backend that cupsd fork+execs per job. ipptool proves nothing about
either.

Candidate explanations, in rough order of likelihood:

 (a) THE DEFAULT QUEUE FLIPPED BACK.  The queue that printed before the reboot
     may not be the queue jobs go to now. If the pre-reboot success was on a
     hand-made queue - especially a plain ipp:// one, which never does TLS at
     all - then client.conf was never load-bearing for printing, and after the
     reboot cups-browsed re-created its implicitclass queue and took the default
     back. This would explain the split perfectly.
     KEY UNKNOWN: which queue actually printed before the reboot? Static
     ipps://? Static plain ipp://? The auto-discovered implicitclass one?

 (b) mDNS NOT READY AT PRINT TIME.  A queue pointing at 246989000000.local
     depends on Avahi being up and multicast working when the job runs. Right
     after a boot that is a race. Failure mode is name resolution, not TLS -
     and it would look identical from the outside ("nothing prints").

 (c) THE BACKEND GENUINELY DOESN'T READ client.conf.  Possible, but note the
     snap's libcups has /var/snap/cups/common/etc/cups compiled in as
     ServerRoot, so the backend should find the file even with no environment
     help. If this is the answer, the error log will show a TLS alert from the
     backend specifically.

 (d) cups-browsed decided reachability at discovery time and cached a verdict.

The error log discriminates between these in one line:
    - "A TLS fatal alert has been received"     -> (c), TLS, backend not capped
    - "No suitable destination host found"      -> (a) or (d), cups-browsed
    - name/host resolution failure              -> (b), mDNS

DIAGNOSTIC SET TO RUN ON HOST (all of it, before theorising further):

    # what queues exist, what are their device URIs, which is default
    cups.lpstat -v
    cups.lpstat -t
    cups.lpstat -o                 # stuck jobs

    # did the config actually survive the reboot, and in what order
    cat /var/snap/cups/common/etc/cups/client.conf
    cat /etc/gnutls/config          # confirm still reverted, no tls1.3 line

    # the logs
    sudo tail -100 /var/snap/cups/current/var/log/error_log
    sudo tail -100 /var/snap/cups/current/var/log/cups-browsed_log

Then the clean separating experiment - a static ipps:// queue, bypassing
cups-browsed entirely (this is old TODO 4.2, which may never have been run in
exactly this form):

    sudo cups.lpadmin -p TS5351ipps -E \
         -v ipps://246989000000.local:631/ipp/print -m everywhere
    cups.lp -d TS5351ipps /etc/hostname

  - prints  => the backend DOES honor client.conf; the problem is which queue
               jobs go to, i.e. candidate (a)/(d), and the fix is queue
               management, not TLS
  - fails   => candidate (c): the backend does not honor client.conf, and the
               scoped fix does not extend to the print path

And to remove (b) from the picture, repeat with the IP instead of the mDNS name:

    sudo cups.lpadmin -p TS5351ip -E \
         -v ipps://192.168.1.50:631/ipp/print -m everywhere
    cups.lp -d TS5351ip /etc/hostname


3c. RESOLVED: HOST RUNS **TWO** CUPS DAEMONS (snap AND deb)
----------------------------------------------------------

This explains the ipptool-works/printing-fails split completely.

Observed on HOST:

    $ ps -ef | grep cupsd
    root  4137     1  /usr/sbin/cupsd -l                          <- DEB
    root  4158     1  /bin/sh /snap/cups/1238/scripts/run-cupsd
    root  4440  4158  cupsd -f -s /var/snap/cups/common/etc/cups/cups-files.conf \
                            -c /var/snap/cups/common/etc/cups/cupsd.conf   <- SNAP

    $ sudo ss -lptne 'sport = :631'
    LISTEN 127.0.0.1:631  cupsd pid=4137  cgroup:.../system-cups.slice/cups.service
    LISTEN [::1]:631      cupsd pid=4137

    $ sudo ss -lpx | grep cups.sock
    /run/cups/cups.sock                  cupsd pid=4137 + systemd pid=1 fd=98
    /var/snap/cups/common/run/cups.sock  cupsd pid=4440

=> the DEB cupsd owns port 631 and the canonical /run/cups/cups.sock.
=> the SNAP cupsd is in PROXY MODE: no TCP port, only its private socket.

THE CONSEQUENCE FOR THE BUG:
  cups.ipptool is a SNAP binary -> snap libcups -> snap client.conf, and when
  given an ipps:// URI it talks STRAIGHT TO THE PRINTER, bypassing every cupsd.
  That is why the snap client.conf capped it and it passed.
  Printing goes to the DEB cupsd, which forks the DEB backend
  /usr/lib/cups/backend/ipp -> apt libcups 2.4.16 -> /etc/cups/client.conf.
  That file is uncapped, so the print path still offers TLS 1.3 and dies.

  => the SSLOptions line is needed in /etc/cups/client.conf for PRINTING.
     The snap's copy only ever fixed snap clients. Both files, or the deb one.
     Caveats: $HOME/.cups/client.conf is tried first by BOTH libcups builds and
     never falls back, so check `ls -la ~/.cups/`; and the deb daemon needs
     `sudo systemctl restart cups`, which restarting the snap does not do.

PROXY MODE MECHANISM - verified by reading /snap/cups/current/scripts/run-cupsd:

  detection (lines ~130-140):
      [ ! -f /var/snap/cups/common/no-proxy ] && [ -r /etc/cups/cupsd.conf ]
    NOTE: keyed on a CONFIG FILE BEING READABLE, not on the deb daemon running.
    So `apt remove cups-daemon` without purging leaves /etc/cups/cupsd.conf
    behind and the snap stays in proxy mode forever, proxying to nothing. The
    script's own comment says to create the no-proxy file in that case.

  in proxy mode:
    - PORT is set empty -> cupsd.conf has every Listen/Port line stripped
    - DOMAINSOCKET = /var/snap/cups/common/run/cups.sock (the ALT socket)
    - client.conf gets `ServerName /var/snap/cups/common/run/cups.sock`
    - $SNAP_DATA/var/run/proxy-mode is touched
    - run-cups-browsed refuses to start cups-browsed while that file exists
    - cups-proxyd <snap socket> <SYSTEM_CUPS_SERVER> -l --logdir ... is spawned,
      mirroring the deb's queues into the snap's cupsd as proxy:// DeviceURIs
    - SYSTEM_CUPS_SERVER is parsed out of /etc/cups/cupsd.conf
      (Listen /socket | Port N | Listen *:N | Listen host:N), default
      localhost:631

  standalone mode, 631 already taken -> falls back to port 10631 plus the alt
  socket ("parallel mode"). Escape hatch: touch /var/snap/cups/common/no-proxy.

  on leaving proxy mode the script deletes leftover proxy:// mirrored queues.

DIAGNOSTIC VALUE:
  - `lpstat -v` vs `cups.lpstat -v` can show the SAME queue names; the snap side
    has proxy:// DeviceURIs. Queue names are not decisive, DeviceURIs are.
  - ls /var/snap/cups/current/var/run/proxy-mode  -> snap is only a proxy
  - snap's client.conf ServerName value tells you the mode:
        /run/cups/cups.sock                 -> standalone
        /var/snap/cups/common/run/cups.sock -> proxy (or parallel)


3d. THE REGRESSION, TAKE THREE - AND A RETRACTION
------------------------------------------------

RETRACTED: "the snap was standalone before the reboot and flipped to proxy mode
after it". Martin confirms HOST had BOTH the deb and the snap installed from the
start, so /etc/cups/cupsd.conf was readable at every snap start and the snap was
in proxy mode before the reboot too. The mode did not change. Dead hypothesis.

CURRENT BEST HYPOTHESIS: the print that worked was reading
/etc/cups/client.conf - the DEB path, which is the correct one for the daemon
that actually owns printing - and that file was cleaned up during the "revert
everything except the snap client.conf" pass. That would leave exactly the
observed state: cups.ipptool (snap libcups, snap client.conf) still passes,
while jobs (deb cupsd -> deb backend -> /etc/cups/client.conf, now absent) fail.
On the VM /etc/cups/client.conf does not exist at all - the deb ships no such
file - which fits "had to be created" in the post.

NEXT ACTION: put SSLOptions MinTLS1.2 MaxTLS1.2 in /etc/cups/client.conf and
`sudo systemctl restart cups` (the DEB unit - restarting the snap does not
touch it). Check `ls -la ~/.cups/client.conf` first: libcups opens that one
first and never falls back.

NEW FINDING - SNAP MODE IS STICKY UNTIL RESTART.  Observed on the VM: the deb
was installed and started at 17:55 while the snap had been running since 16:11.
Result - deb cups + cups-browsed active, /etc/cups/cupsd.conf readable, and yet
the snap was STILL in standalone mode: no proxy-mode marker, client.conf still
`ServerName /run/cups/cups.sock`, snap cups-browsed still running (proxy mode
would have blocked it). run-cupsd evaluates the mode once, at snap service
start. So installing or removing the deb changes nothing until the next
`snap restart cups` or reboot. Good blog material even though it did not explain
the HOST regression.

ALSO: with the deb installed the VM now has TWO of every CLI tool -
/usr/bin/ipptool and /usr/bin/lpstat from cups-ipp-utils / cups-client next to
cups.ipptool and cups.lpstat. They read DIFFERENT client.conf files. And note
`cups-config` is NOT in the deb runtime set at all (it lives in libcups2-dev),
so the "ask cups-config --serverroot" advice now in the post's lesson 7 only
works for the snap - worth fixing.

VM is being rebooted to match HOST (both installed, both started at boot).
Probe script for after the reboot: ./cups-probe.sh in this repo - read-only,
run with sudo, dumps everything in sections.


4. TODO / TO CHECK  (in this order)
-----------------------------------

[x] 4.1 CONTROL TEST - is client.conf really the cause?  (HOST)  DONE
        Settled by a stronger test than the one planned: all gnutls config
        changes were reverted, leaving the snap client.conf line as the only
        host modification, and it still works. No confound.

[x] 4.2 DOES THE BACKEND HONOR IT?  (HOST)  DONE
        Yes - printing works, not just cups.ipptool. The forked ipp backend
        picks up the snap's client.conf.

[ ] 4.2b REBOOT TEST  (HOST, in progress)
        Martin is restarting. Expect it to hold: $SNAP_COMMON persists and
        run-cupsd preserves the line (see section 3, DURABILITY). Record the
        result here.

[ ] 4.3 THE cups-browsed QUEUE  (HOST, optional now)
        Less urgent than it looked: with TLS 1.2 negotiating, the
        auto-discovered implicitclass queue should work on its own. Only needed
        if a stale broken queue is still hanging around. Original notes:
        cups-browsed persists remembered queues to a cache file, so a plain
        restart can resurrect the broken implicitclass queue instead of
        rediscovering cleanly:

            sudo cups.lpadmin -x Canon_TS5300_series
            sudo snap stop cups
            sudo ls -la /var/snap/cups/current/var/cache/    # find the file
            sudo rm -f /var/snap/cups/current/var/cache/cups-browsed_cache
            sudo snap start cups

        The cache filename is UNCONFIRMED - AppArmor blocks reading that
        directory even from inside the snap namespace. List it first.

[ ] 4.4 RESOLVE THE nm DISCREPANCY  (VM can do this; needs a source read)
        The post's "Attempt two" claims libcups never asks GnuTLS for the system
        default, resting on there being exactly one priority symbol. That is
        true of the apt library but NOT of the snap's:

            apt  /usr/lib/x86_64-linux-gnu/libcups.so.2:
                 U gnutls_priority_set_direct
            snap /snap/cups/current/lib/libcups.so.2:
                 U gnutls_priority_set_direct
                 U gnutls_set_default_priority        <-- extra

        gnutls_set_default_priority IS the "ask for the system default" call.
        If the snap's libcups reaches it on any path CUPS actually takes, then
        (a) that sentence in the post is wrong for the snap, and (b)
        default-priority-string in /etc/gnutls/config may not be inert for CUPS
        after all. Needs a look at CUPS 2.4 tls-gnutls.c to see which branch
        calls which. Do NOT retarget the post's nm command at the snap library
        until this is settled - the output would contradict the prose above it.


5. WHAT THIS DOES TO THE BLOG POST
----------------------------------

Committed so far: b30654a added the whole snap section (Step 4 opener, path
table, proxy mode, why /etc/gnutls/config reaches inside the snap).

Uncommitted in the working tree right now: the client.conf path fixes -
attempt one now names /var/snap/cups/common/etc/cups/client.conf as the file the
daemons read, both ipptool invocations became cups.ipptool, and lesson 7 gained
the "make sure it's the same client.conf" note.

DONE (uncommitted): the cups.ipptool finding is now folded in, and ONLY that.
  - attempt one retitled "client.conf (half a fix)"
  - the false "made no difference in the right file either" paragraph is gone;
    replaced with the PASS output plus an explicit split - client path fixed,
    printing not fixed, holdout process unknown
  - attempt two gained a bridging sentence: the override wins because it bites
    below the priority string, so it reaches processes that never open a
    client.conf
  - the scoped-fix bullet list gained a first bullet crediting client.conf with
    part of the ground
  - lesson 7 reworded: it now covers both failure shapes (never read / read but
    jobs still do not print)
  No claim anywhere that printing is fixed. The transient pre-reboot success is
  deliberately NOT mentioned in the post - it is unexplained, see 3b.

STATUS: full restructure still ON HOLD. The post-reboot regression means we do not yet
know whether client.conf fixes PRINTING or only the client path. The post's
current narrative - attempt one didn't work, the gnutls override did, the static
plain-IPP queue is the pragmatic fix - may turn out to be closer to correct than
it looked. Do not restructure until section 3b is resolved.

The uncommitted paragraph asserting "it made no difference in the right file
either / attempt one fails on the correct path too" is still not safe to commit:
it is at best unproven and at worst backwards. Either resolve 3b first or drop
that paragraph to something narrower.

Rewrite that WOULD be needed if client.conf turns out to fix printing:

  - attempt one becomes the fix, not a dead end. What misled the original
    debugging was testing the classic path / a host ipptool.
  - "Attempt two: the GnuTLS override, which works" gets demoted from THE fix
    to a detour with too much blast radius.
  - the post's closing wish ("a properly scoped version is what I'm looking for
    now", the GNUTLS_SYSTEM_PRIORITY_FILE thread) gets an actual answer:
    SSLOptions MaxTLS1.2 in the snap's client.conf. That is a better ending
    than the one the post currently has.
  - Step 7's static plain-IPP queue stays valid either way, but stops being
    "the pragmatic fix beat the correct fix" - the correct fix would now be
    two lines and no blast radius.
  - lesson 8 ("the pragmatic fix beat the correct fix by an order of
    magnitude") needs rewording for the same reason.

Do not rewrite any of that until 4.1 and 4.2 have actually been run on HOST.
